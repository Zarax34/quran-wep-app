# دليل استخدام نظام إدارة مركز الإمام حفص (للمطورين والذكاء الاصطناعي)

هذا المستند يقدم تحليلاً شاملاً ودقيقاً لنظام إدارة مركز الإمام حفص لتحفيظ القرآن الكريم. الهدف منه هو توفير سياق كامل للذكاء الاصطناعي لفهم بنية النظام، قواعد البيانات، والعمليات المنطقية التي تحدث في الخلفية.

---

## 1. نظرة عامة على النظام (System Architecture)

النظام مبني باستخدام **Flask (Python)** كواجهة خلفية (Backend) ويعتمد على **SQLite** كقاعدة بيانات (مع دعم MySQL). الواجهة الأمامية (Frontend) تستخدم **Jinja2 Templates** مع **Bootstrap 5** للتصميم المتجاوب.

### الأدوار والصلاحيات (User Roles)
النظام يعتمد على صلاحيات محددة لكل دور:
1.  **المسؤول (Admin):** تحكم كامل في النظام (الإعدادات، المستخدمين، الحلقات، التقارير، الحذف والتعديل).
2.  **المعلم (Teacher):** إدارة حلقاته فقط (عرض طلابه، إضافة تقارير، تحضير، طلب عطل).
3.  **ولي الأمر (Parent):** عرض بيانات أبنائه فقط (لوحة تحكم خاصة، تقارير، تواصل).
4.  **الطالب (Student):** عرض بياناته الشخصية وتقاريره.
5.  **مسؤول التواصل (Communication Officer):** دور إشرافي يشبه المعلم ولكن بصلاحيات أوسع قليلاً في التقارير (اعتماد).

---

## 2. هيكلية البيانات (Database Schema)

فيما يلي شرح دقيق لكل جدول (Model) والحقول التي يتكون منها:

### `User` (المستخدمون)
*   `id`: المعرف الفريد.
*   `username`: اسم المستخدم للدخول.
*   `password`: كلمة المرور (مشفرة).
*   `role`: الدور (`admin`, `teacher`, `parent`, `student`, `support`).
*   `name`: الاسم الظاهر.
*   `fingerprint_id`: معرف البصمة (اختياري).

### `Student` (الطلاب)
*   `id`: المعرف.
*   `name`: الاسم الرباعي.
*   `circle_id`: معرف الحلقة (ForeignKey).
*   `parent_id`: معرف ولي الأمر (ForeignKey).
*   `parent_phone`: رقم هاتف ولي الأمر (يستخدم للتواصل).
*   `student_phone`: رقم الطالب.
*   `total_verses_since_year_start`: عداد تراكمي للآيات المحفوظة (يحدث تلقائياً عند إضافة تقرير حفظ).
*   `last_memorized_sura`, `last_memorized_ayah`: آخر موضع حفظ.
*   `pending_approval`: حالة الطالب (هل يحتاج موافقة أدمن؟).

### `Report` (التقارير اليومية)
*   `type`: نوع التقرير (`حفظ`, `مراجعة`, `تلقين`, `تلاوة`).
*   `surah`, `from_verse`, `to_verse`: نطاق التسميع.
*   `grade`: التقدير (`ممتاز` = 10 نقاط, `جيد جدا` = 7, إلخ).
*   `status`: حالة التقرير (`Approved` للأدمن, `Pending` للمعلم). *ملاحظة: النقاط تضاف فوراً عند الإنشاء في الكود الحالي.*

### `Attendance` (الخضور والغياب)
*   `status`: الحالة (`حاضر`, `غائب بعذر`, `غائب بلا عذر`, `هروب`, `لم يسمع`). *ملاحظة: حالة "لم يسمع" تعامل كحضور إحصائياً.*

### `Settings` (الإعدادات العامة)
يخزن إعدادات الموقع في سطر واحد:
*   `permissions`: حقل نصي JSON يخزن صلاحيات المعلمين المتقدمة (مثل `teacher_view_phone`) وتخصيص شريط التنقل للموبايل.
*   `whatsapp_message_template`: نص رسالة الواتساب الديناميكية.

---

## 3. شرح مفصل للصفحات والحقول (Page & Field Analysis)

### أ. تسجيل الدخول (`/login`)
*   **Username:** اسم المستخدم.
*   **Password:** كلمة المرور.
*   **المنطق الخلفي:** عند النجاح، يتم توجيه المستخدم حسب دوره (`dashboard` للأدمن/المعلم، `parent_dashboard` للأب، `student_dashboard` للطالب).

### ب. لوحة التحكم (`/dashboard`)
*   **الإحصائيات (Cards):** تعرض إجمالي الطلاب، المعلمين، الحلقات، وتقارير اليوم. تعتمد على `CountUp.js` للأنيميشن.
*   **الرسم البياني للحضور:** يعرض نسبة الحضور لكل حلقة (أسبوعياً).
*   **طالب الشهر:** يتم حسابه تلقائياً بناءً على عدد الشارات (`Badge`) المكتسبة هذا الشهر.
*   **الإجراءات السريعة:** أزرار اختصار تختلف حسب الدور (مثلاً: المعلم يرى "تقرير جماعي"، الأدمن يرى "الحلقات").

### ج. إدارة الطلاب (`/students` & `/add_student`)
صفحة `/add_student`:
1.  **الاسم الكامل (Name):** نصي. يستخدم لإنشاء اسم المستخدم للطالب تلقائياً.
2.  **رقم ولي الأمر (Parent Phone):** ضروري جداً.
    *   *منطق خفي:* النظام يبحث عن ولي أمر موجود بهذا الرقم. إذا لم يجده، ينشئ حساب ولي أمر جديد (`Parent` + `User`) تلقائياً.
    *   *كلمة مرور ولي الأمر:* تكون رقم الهاتف نفسه.
3.  **الحلقة (Circle):** قائمة منسدلة. المعلم يرى حلقاته فقط.
4.  **تاريخ الميلاد & العنوان:** بيانات ديموغرافية.
5.  **آخر حفظ (Last Memorized):** لتحديد نقطة البداية.
6.  **الصورة (Photo):** رفع صورة شخصية.

### د. التقرير الجماعي (`/collective_report`) - *ميزة معقدة*
هذه الصفحة (`collective_report_form.html`) هي الأداة الرئيسية للمعلم:
*   **التاريخ:** افتراضياً اليوم.
*   **الجدول (صفوف الطلاب):**
    *   **الحالة (Status):** قائمة منسدلة (`حاضر`, `غائب`, ...). تغييرها يؤثر على ظهور حقول التسميع.
        *   *تأثير:* إذا اخترت "هروب" أو "غائب"، يتم إرسال إشعار (`Notification`) للأب فوراً.
    *   **النوع (Type):** حفظ/مراجعة.
    *   **السورة والآيات:** يتم ملؤها تلقائياً بناءً على `student_expectations` (توقع الحفظ الجديد بناءً على آخر حفظ).
    *   **زر (+):** يسمح بإضافة تسميع آخر لنفس الطالب في نفس اليوم (مثلاً: حفظ + مراجعة). يتم إرسال البيانات كمصفوفات (`surah[]`).
*   **الملاحظة (Note):** ملاحظة نصية تحفظ مع سجل الحضور.
*   **الحفظ:** يقوم السيرفر بإنشاء سجلات `Attendance` وسجلات `Report` متعددة لكل صف مضاف. يتم تحديث `total_verses` للطالب إذا كان النوع "حفظ".

### هـ. إدارة الإعدادات (`/settings`)
صفحة التحكم الكاملة للمسؤول:
1.  **المعلومات الأساسية:** اسم الموقع، الوصف، أرقام التواصل.
2.  **المظهر:** رفع الشعار، اختيار الألوان (Primary/Secondary).
3.  **الصلاحيات (Permissions Switches):**
    *   `السماح للمعلمين برؤية أرقام الهواتف`: يتحكم في إخفاء/إظهار الرقم في صفحة تفاصيل الطالب.
    *   `السماح بإرسال التقارير`: يفتح/يغلق ميزة الإرسال.
4.  **تخصيص شريط التنقل (Mobile Nav):**
    *   قائمة (Checkboxes) لاختيار 4 أيقونات تظهر في الشريط السفلي في عرض الجوال. تحفظ كـ JSON في قاعدة البيانات.
5.  **قالب الواتساب:** نص مع متغيرات (مثل `{student_name}`) يستخدم لتوليد رابط `wa.me` لإرسال التقارير.

### و. الأنشطة (`/activities`)
*   **المشاركون:** يمكن تحديد طلاب محددين أو حلقات كاملة.
*   **الموافقة (`ActivityApproval`):** تتطلب بعض الأنشطة موافقة ولي الأمر. تظهر في لوحة تحكم ولي الأمر كبطاقات للموافقة/الرفض.

### ز. الرسوم (`/fees`)
*   **توليد الرسوم (`/generate_monthly_fees`):** يسمح للأدمن بإنشاء مطالبات مالية لمجموعة حلقات دفعة واحدة.
*   **الحالة:** `Paid` أو `Pending`.

---

## 4. العمليات الخلفية والمنطق الخفي (Business Logic)

### نظام النقاط والشارات (Gamification)
*   **النقاط:** تمنح تلقائياً عند إضافة تقرير:
    *   ممتاز = 10 نقاط.
    *   جيد جداً = 7 نقاط.
*   **الشارات (Badges):** دالة `check_for_badges(student_id)` تعمل بعد كل تقرير وتمنح شارات تلقائياً:
    *   *المتقن:* 10 تقارير "ممتاز".
    *   *الحافظ:* حفظ 500 آية.
    *   *الحاضر المثالي:* حضور 30 يوم متتالي.

### الإشعارات (Notifications)
*   يتم إنشاء إشعارات داخلية للأب (`User.notifications`) في الحالات التالية:
    1.  تسجيل غياب أو هروب للطالب.
    2.  إضافة تقرير جديد.
    3.  دعوة لنشاط (Activity).

### المزامنة والوضع الليلي
*   **الوضع الليلي:** يمكن تفعيله من الإعدادات أو زر في القائمة. يعتمد على CSS Variables ويخزن التفضيل في الجلسة (`session`).
*   **عرض الموبايل:** النظام يكشف الـ User-Agent ويحول العرض تلقائياً، أو يمكن للمستخدم التبديل يدوياً (`/set_view`). في وضع الموبايل، تختفي القائمة الجانبية ويظهر شريط سفلي (`parent-bottom-nav`).

### الترحيل من SQLite إلى MySQL
*   النظام مصمم ليعمل بـ SQLite افتراضياً، لكن توجد سكربتات ترحيل (`scripts/`) لدعم الانتقال لقواعد بيانات أكبر.

---

## 5. ملاحظات تقنية هامة (For Developers)
*   **حساب الصفحات:** الدالة `calculate_pages` تحسب عدد الصفحات بين آيتين تقريبياً، وتستخدم في حساب إنجاز الخطة الشهرية.
*   **التقارير المتعددة:** عند استخدام "إضافة تقرير" بنمط النطاق (من سورة X إلى سورة Y)، يتم إنشاء سجل تقرير *واحد* ولكن يتم حساب مجموع الآيات وإضافته للإحصائيات، وتفاصيل النطاق تضاف في الملاحظات.
*   **الحذف المتسلسل:** حذف "دورة" (`Course`) يحذف تلقائياً كل التسجيلات والاختبارات المرتبطة بها (Cascading Delete).

هذا المستند يغطي الجوانب الوظيفية والتقنية بدقة، مما يتيح فهم النظام بالكامل.
