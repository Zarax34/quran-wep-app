# دليل استخدام نظام إدارة مركز الإمام حفص (للمطورين والذكاء الاصطناعي)

هذا المستند يوفر توثيقاً دقيقاً جداً (Field-Level Documentation) لكل زر، حقل، وميزة في النظام. تم تصميمه ليكون مرجعاً تقنياً للذكاء الاصطناعي لفهم المنطق الدقيق للواجهة والعمليات الخلفية.

---

## 1. شاشة تسجيل الدخول (`/login`)

الواجهة الأولى للنظام. تحتوي على تصميم عصري مع دعم للخلفيات المخصصة.

### الحقول والأزرار:
*   **اسم المستخدم (`username`):** حقل نصي.
*   **كلمة المرور (`password`):** حقل نصي (نوع password).
*   **زر "تسجيل الدخول":** زر (Submit) يقوم بإرسال نموذج POST.
    *   *المنطق الخلفي:* يتحقق من صحة البيانات باستخدام `check_password_hash`. في حال النجاح، يتم إنشاء جلسة (`session`) وتوجيه المستخدم بناءً على دوره (`role`).
*   **زر "نسيت كلمة المرور؟":** رابط يوجه لصفحة الدعم (`/support`).
*   **زر الدخول كضيف (أيقونة المستخدم):** رابط يوجه لـ `/guest_dashboard` لعرض إحصائيات عامة للزوار.

---

## 2. لوحة التحكم الرئيسية (`/dashboard`)

مركز العمليات الرئيسي للمسؤولين والمعلمين. تختلف المحتويات بناءً على الدور.

### أ. البطاقات الإحصائية (Stats Cards):
تستخدم مكتبة `CountUp.js` للعد التنازلي.
1.  **إجمالي الطلاب:** يعرض عدد الطلاب النشطين (`is_active=True`).
2.  **إجمالي المعلمين:** يعرض عدد المستخدمين بدور `teacher`.
3.  **عدد الحلقات:** إجمالي الحلقات النشطة.
4.  **التقارير اليوم:** يعرض عدد التقارير التي تم إنشاؤها بتاريخ "اليوم".

### ب. قسم الإجراءات السريعة (Quick Actions):
أزرار تنقل سريع تختلف حسب الصلاحية:
*   **للمعلم/الأدمن:**
    *   `تقرير جماعي`: يوجه لصفحة اختيار الحلقة، ومنها لنموذج التقرير الجماعي.
    *   `الحضور`: يوجه لصفحة رصد الحضور.
    *   `إضافة طالب`: يوجه لصفحة التسجيل الجديد.
    *   `تقرير فردي`: يوجه لصفحة إضافة تقرير لطالب واحد.
*   **للأدمن فقط:**
    *   `الحلقات`: يوجه لإدارة الحلقات.
*   **عام:**
    *   `قائمة الطلاب`: عرض الكل.
    *   `جميع التقارير`: أرشيف التقارير.
    *   `ادعم المركز`: صفحة التبرع.

### ج. قسم "آخر التقارير" (Recent Reports):
*   يعرض جدولاً (على الكمبيوتر) أو بطاقات (على الهاتف) لآخر 10 تقارير.
*   يتم تجميع التقارير حسب "الحلقة" باستخدام `groupby` في القالب، مما يظهرها في قوائم منسدلة (Accordion).

### د. طالب الشهر (Student of the Month):
*   يعرض بطاقات للطلاب الذين حققوا أعلى عدد من الشارات (`Badges`) خلال الشهر الحالي.
*   يتم حسابه تلقائياً عبر استعلام قاعدة البيانات (`func.max` على `StudentBadge`).

---

## 3. إدارة الطلاب (`/students` & `/add_student`)

### أ. صفحة إضافة طالب (`add_student.html`):
تحتوي على نموذج شامل يربط بين الطالب، ولي الأمر، والحلقة.

**الحقول بالتفصيل:**
1.  **الاسم الكامل (`name`):**
    *   *النوع:* نصي (Text).
    *   *الأهمية:* يستخدم كاسم عرض للطالب، ويستخدم أيضاً لإنشاء `username` للطالب تلقائياً.
2.  **اسم ولي الأمر (`parent_name`):**
    *   *المنطق الخفي:* هذا الاسم يستخدم لإنشاء حساب مستخدم (`User`) لولي الأمر إذا لم يكن موجوداً. اسم المستخدم لولي الأمر سيكون هو هذا الاسم تماماً.
3.  **رقم ولي الأمر (`parent_phone`):**
    *   *النوع:* رقم (Tel).
    *   *منطق حساس:* النظام يبحث في جدول `Parent` عن هذا الرقم.
        *   إذا وجد: يربط الطالب بولي الأمر الموجود.
        *   إذا لم يجد: ينشئ سجل `Parent` جديد + سجل `User` جديد (بكلمة مرور = رقم الهاتف).
4.  **الحلقة (`circle_id`):** قائمة منسدلة (`Select`). تظهر الحلقات النشطة فقط. المعلم يرى حلقاته فقط.
5.  **آخر سورة/آية (`last_memorized_sura`, `last_memorized_ayah`):**
    *   تستخدم لتحديد نقطة انطلاق الطالب في الحفظ.
6.  **اتجاه الحفظ (`memorization_direction`):**
    *   الخيارات: `BaqarahToNas` (من البقرة إلى الناس) أو `NasToBaqarah` (العكس).
    *   *التأثير:* يؤثر على المنطق التلقائي لتحديد ما إذا كان التقرير "حفظ" أم "مراجعة" في المستقبل.
7.  **بيانات ديموغرافية:** (العمر، السكن الحالي، السكن السابق، المحافظة، تاريخ الميلاد). حقول نصية/تاريخ للحفظ فقط.
8.  **الصورة (`photo`):** رفع ملف. يقبل (png, jpg, jpeg).

### ب. صفحة عرض الطلاب (`students.html`):
*   **الفلترة:** يمكن تصفية القائمة حسب الحلقة.
*   **البحث:** حقل بحث فوري يبحث في (اسم الطالب، هاتف الطالب، اسم الأب، هاتف الأب).
*   **الإجراءات (لكل طالب):**
    *   `تعديل`: يفتح نموذج التعديل.
    *   `حذف`: يضع علامة `is_active=False` (حذف ناعم Soft Delete).
    *   `نقل`: زر يفتح نافذة لاختيار حلقة جديدة لنقل الطالب إليها.

---

## 4. نظام التقارير الجماعية (`/collective_report`)

الميزة الأكثر تعقيداً وذكاءً في النظام. تسمح برصد الحفظ، المراجعة، والحضور لكل الفصل في شاشة واحدة.

### الواجهة (`collective_report_form.html`):
1.  **حقل التاريخ (`date`):** يحدد تاريخ التقرير (افتراضياً اليوم).
2.  **قائمة الطلاب (Rows/Cards):**
    *   يتم عرض الطلاب مرتبين حسب الإنجاز (عدد الآيات المحفوظة).
    *   على الهاتف، تظهر القائمة كـ "أكورديون" (Accordion)، بحيث يفتح طالب واحد فقط في كل مرة لتوفير المساحة.

### تفاصيل الصف لكل طالب:
1.  **قائمة الحالة (`status`):**
    *   الخيارات: `حاضر`, `لم يسمع`, `غائب بعذر`, `غائب بلا عذر`, `هروب`.
    *   *المنطق التفاعلي (JS):* تغيير الحالة يؤثر فوراً على ظهور حقول التسميع. إذا اختير أي خيار غير "حاضر"، تختفي حقول السورة والآيات.
    *   *المنطق الخلفي:* اختيار "هروب" أو "غائب" يرسل إشعاراً فورياً لولي الأمر.
2.  **زر الإضافة (+):**
    *   يسمح بإضافة صف تسميع جديد لنفس الطالب (مثلاً: الطالب سمع "حفظ" ثم سمع "مراجعة").
    *   يستخدم JavaScript لاستنساخ حقول الإدخال.
3.  **حقول التسميع (مكررة لكل صف):**
    *   **النوع (`type[]`):** `حفظ`، `مراجعة`، `تلقين`.
    *   **السورة (`surah[]`):** قائمة منسدلة بأسماء السور.
        *   *ميزة ذكية:* يتم اختيار السورة المتوقعة تلقائياً (`selected`) بناءً على آخر تقرير للطالب (Variable: `student_expectations`).
    *   **من آية / إلى آية (`from_verse[]`, `to_verse[]`):** أرقام الآيات.
4.  **التقدير (`grade`):** قائمة منسدلة (`ممتاز`, `جيد جداً`, ...).
5.  **ملاحظة يومية (`note`):** حقل نصي لكتابة ملاحظة خاصة بهذا اليوم.

---

## 5. إدارة الأنشطة (`/activities`)

### أ. عرض الأنشطة (`activities.html`):
*   تعرض الأنشطة كبطاقات (Cards) تحتوي الصورة، العنوان، والتاريخ.
*   **منطق الأدمن:** يرى زر "تعديل" و "حذف".
*   **منطق ولي الأمر:** يرى زر "عرض تفاصيل المشاركة" للأنشطة التي دعي ابنه إليها. عند الضغط، تفتح نافذة منبثقة (Modal).

### ب. النافذة المنبثقة للموافقة (Parent Modal):
*   تظهر فقط لولي الأمر.
*   تحتوي على تفاصيل النشاط وزرين:
    *   **موافقة (Approve):** ترسل طلب API لتحديث حالة `ActivityApproval` إلى `Approved`.
    *   **رفض (Reject):** تحدث الحالة إلى `Rejected`.

### ج. إضافة نشاط (`add_activity.html`):
1.  **التفاصيل الأساسية:** العنوان، الوصف، التاريخ، وقت البدء/الانتهاء، الرسوم، الصورة.
2.  **الحلقات المستهدفة (`circle_ids`):** قائمة متعددة الاختيار (Multiple Select). تحدد الحلقات المدعوة بالكامل.
3.  **الطلاب المشاركون (`student_ids`):** قائمة متعددة الاختيار لدعوة طلاب محددين (حتى لو لم تكن حلقتهم مدعوة).
    *   *المنطق:* النظام ينشئ سجلات `ActivityApproval` لكل طالب مدعو (سواء عبر الحلقة أو فردياً) ويرسل إشعارات لأولياء أمورهم.

---

## 6. الإعدادات (`/settings`)

لوحة التحكم المركزية لضبط سلوك النظام.

### أ. مفاتيح التبديل (Toggles):
1.  **السماح للمعلمين برؤية أرقام الهواتف:**
    *   يتحكم في المتغير `teacher_view_phone` في JSON الصلاحيات. إذا عُطل، سيظهر "غير مصرح" مكان رقم الهاتف في حساب المعلم.
2.  **السماح للمعلمين بإرسال التقارير:**
    *   يتحكم في زر "إرسال واتساب" في التقارير.
3.  **الموافقة على المعلمين/الطلاب:**
    *   إذا فُعل، فإن أي طالب يضاف بواسطة معلم يكون في حالة `pending_approval` ولا يظهر في القوائم العامة حتى يعتمده الأدمن.

### ب. تخصيص شريط التنقل (Mobile Navigation):
*   يحتوي على مجموعة `Checkboxes` تمثل جميع صفحات النظام (الطلاب، الحلقات، الرسوم، إلخ).
*   يسمح للأدمن باختيار 4 أيقونات فقط لتظهر في الشريط السفلي الثابت على الجوال.
*   باقي الأيقونات ترحل تلقائياً إلى قائمة "المزيد".

### ج. قالب الواتساب:
*   محرر نصي (`textarea`) يسمح بكتابة نص الرسالة التي سترسل لولي الأمر.
*   يدعم متغيرات الاستبدال: `{student_name}`, `{grade}`, `{surah}`, إلخ.

---

## 7. الرسوم المالية (`/fees`)

### أ. توليد الرسوم (`generate_monthly_fees`):
أداة للأدمن لإنشاء مطالبات مالية جماعية.
*   **العنوان:** اسم الرسم (مثلاً: "رسوم شهر أكتوبر").
*   **المبلغ:** القيمة المطلوبة.
*   **الحلقات:** اختيار الحلقات التي سيتم فرض الرسوم على طلابها.
*   *العملية:* ينشئ النظام سجلاً في جدول `Fee` لكل طالب في الحلقات المختارة بحالة `Pending`.

### ب. دفع الرسوم:
*   في صفحة الرسوم، يظهر زر "تأكيد الدفع" أمام الرسوم غير المدفوعة.
*   عند الضغط، يتم تحديث الحالة إلى `Paid` وتاريخ الدفع إلى تاريخ اليوم.

---

## 8. العمليات الخلفية (Backend Processes)

1.  **إنشاء ولي الأمر التلقائي:** عند إضافة طالب برقم هاتف جديد، يتم إنشاء `Parent` و `User` تلقائياً.
2.  **تحديث عداد الآيات:** عند إضافة تقرير نوعه "حفظ"، يتم حساب عدد الآيات (`to_verse - from_verse + 1`) وإضافته لرصيد الطالب.
3.  **جدولة المهام (Scheduler):**
    *   فحص الإعلانات (`check_upcoming_events`): يرسل إشعارات يومية قبل موعد الإعلان بـ 7 أيام.
    *   التقارير الأسبوعية (`send_weekly_progress_reports`): يرسل ملخصاً بالبريد الإلكتروني (إن وجد) كل أحد.

هذا التوثيق يغطي بدقة متناهية كل جزء في النظام لضمان فهم كامل لطريقة عمله برمجياً ووظيفياً.
